# Exercise 3 — Contact Links

Create `contact-links.html`:
- Add a `mailto:` link with a visible address.
- Add a `tel:` link with a visible phone number.
- Use clear link text (e.g., the address or number itself).

Checklist:
- Links are correctly formatted and clickable.
- Link text is descriptive.
