# Exercise 2 — Fix the Hierarchy

Open `broken-headings.html` (create it):
Copy this incorrect structure, then fix it:
```
<h1>My Pets</h1>
<h3>Cats</h3>
<p>I have two cats.</p>
<h5>Dogs</h5>
<p>I have one dog.</p>
```
Make levels consistent and logical.

Checklist:
- One `<h1>`.
- No jumps (e.g., `<h1>` to `<h3>` without `<h2>` if a new subsection).
