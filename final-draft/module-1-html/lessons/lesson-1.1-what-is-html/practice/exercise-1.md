# Exercise 1 — Your First Page

Create `my-first-page.html` in `student-workspace`:
- Add a meaningful `<title>`.
- Include one `<h1>` and one `<p>`.
- Save and open in your browser.

Checklist:
- Document structure present (doctype/html/head/body).
- Title reflects the page.
- Content is readable and makes sense.
