# Exercise 1: Create a Styled Playing Card

## 🎯 Goal
Create a single playing card (Ace of Spades) with proper styling, including:
- Correct dimensions and proportions
- Rounded corners
- Shadow for depth
- Black suit color
- Corner decorations

## 📋 Requirements

1. Create an HTML file with a single card
2. Use the Unicode symbol for spades (♠)
3. Style the card with:
   - Width: 80px, Height: 112px
   - Border radius: 8px
   - Box shadow for depth
   - Center the rank and suit
4. Add small rank/suit indicators in the top-left corner

## 💡 Hints

- Use `display: flex` to center content
- The spades symbol is `♠` or `&spades;`
- Use `::before` pseudo-element for corner decorations
- Black color for spades: `#333`

## ✅ Success Criteria

Your card should:
- Look like a real playing card
- Have proper proportions
- Display clearly on the page
- Have rounded corners and shadow

Check your work against `answer-key/exercise-1-solution.html`!
